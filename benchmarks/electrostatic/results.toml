# Auto-generated by `cargo run -p geode-core --release \
#   --example electrostatic_capacitance`.
# Do NOT edit by hand — regenerate after any intentional change.
# Consumed by `tests/electrostatic_capacitance.rs`.

[meta]
description = "Electrostatic solver + Maxwell capacitance-matrix extraction (Epic #475, Palace Electrostatic parity): 3-D P1 tet scalar solve of -div(eps grad phi)=rho with per-element eps_r and multi-conductor Dirichlet electrodes; N unit-voltage solves -> C_ij = phi_i^T K phi_j (full K). Programmatically-meshed coax + concentric-sphere oracles at <=1%, two-sphere off-diagonal at an honest box-truncation band."
method = "energy_reaction"  # C_ij = phi_i^T K phi_j, full unconstrained K
cross_check = "surface_flux"  # Q = closed-surface integral of eps*(-grad phi).n, looser band
element = "p1_tet"
permittivity_f_per_m = 8.8541878128e-12  # eps_0

[oracles.coax]
# C/L = 2*pi*eps_0*eps_r / ln(b/a)
inner_radius_a = 1
outer_radius_b = 2.5
eps_r = 1.0
nodes = 2304
tets = 6144
c_per_length_fem = 6.080726179e-11
c_per_length_exact = 6.071490285e-11
rel_err = 1.521191e-3  # bar: <= 1e-2
c_per_length_flux = 5.588870616e-11
flux_rel_err = 7.948949e-2  # looser sanity band (~8%), not the bar

[oracles.concentric_spheres]
# C = 4*pi*eps_0*eps_r * a*b/(b-a)
inner_radius_a = 1
outer_radius_b = 2
eps_r = 1.0
nodes = 17934
c_fem = 2.236727237e-10
c_exact = 2.225300111e-10
rel_err = 5.135095e-3  # bar: <= 1e-2
rel_err_coarse = 2.083053e-2  # n_r = 3 (vs fine n_r = 6)
convergence_order = 2.0202  # ~2 => O(h^2) P1 energy
c_flux = 1.902094727e-10
flux_rel_err = 1.452413e-1  # wide sanity band (~15%), not the bar

[oracles.two_sphere_box]
# Two conductor spheres in a grounded box; off-diagonal Maxwell term.
# Reference: leading image-charge term |C_12| ~ 4*pi*eps_0*r^2/sep, with
# higher-order images + finite-box truncation + staircase-sphere
# discretization setting an HONEST wide band (not 1%).
sphere_radius = 0.5
separation = 2
box_half_side = 5
nodes = 68921
c_aa = 5.299704620e-11
c_ab = -8.190910962e-12
c_ba = -8.190910962e-12
c_bb = 5.299704620e-11
max_rel_asymmetry = 0.000e0  # hard check: < 1e-9
c_mutual_abs = 8.190910962e-12
c_mutual_image_leading = 1.390812569e-11
mutual_rel_err_vs_image = 4.110701e-1  # honest band, box-truncation-dominated

[oracles.palace]
status = "pending_operator_run"
note = "Palace's Electrostatic module extracts the same terminal capacitance matrix (terminal-C.csv). Palace is not installed on the generation machine (Docker build recipe only, per #476's verified note), so this slot stays pending until an operator runs Palace. PalaceResults currently parses s-parameters.csv; populating this slot needs either a small terminal-C.csv ingester extension or a manually-populated block carrying palace_version + config_sha256 provenance. Same toolchain-gap convention as the FastHenry slot of benchmarks/spiral_inductor/results.toml and the Palace slot of benchmarks/patch_antenna/results.toml. Tolerance band when populated: 1% on the coax/sphere diagonal C, honest few-% on the two-sphere off-diagonal."
