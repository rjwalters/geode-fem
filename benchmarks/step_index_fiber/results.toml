# Auto-generated by `cargo run -p geode-core --release \
#   --example step_index_fiber`.
# Do NOT edit by hand — regenerate after any intentional change.
# Consumed by `tests/step_index_fiber_benchmark.rs` and compared
# against the EXACT LP-mode oracle (geode_core::fiber_lp, Phase 2A).

[meta]
description = "SMF-28 step-index fiber benchmark (issue #314, Epic #303 Phase 2C, completes Phase 2): genuine fundamental LP01/HE11 mode of a 4.1 um core (n_core=1.4504) in cladding (n_clad=1.4447) at 1550 nm, via a generous open boundary on a disk mesh, vs the EXACT Bessel-function LP-mode characteristic equation (Phase 2A). The HEADLINE validation is on the normalized b = (n_eff^2 - n_clad^2)/(n_core^2 - n_clad^2): the 0.39%-wide window makes n_eff agreement a squeezed-window artifact, so n_eff is reported only as window-limited context and b is the real discriminator."
generated_at_commit = "09bc0699df1005b2b8268a564854bd10cd6a77c6"
lambda_um = 1.55
k0_per_um = 4.053667940115862e0
mode = "LP01 (HE11), fundamental"
mode_index = 0
outer_boundary = "pec (far cladding circle)"
single_mode = true
notes = [
  "Cross-section: circular step-index fiber, core radius a = 4.1 um (n_core = 1.4504) in cladding (n_clad = 1.4447), embedded in a generous cladding disk terminated by a far PEC circle. Per-triangle epsilon via disk_tri_mesh region tags (Phase 2B) + epsilon_r_from_region_tags (Phase 1A); fundamental n_eff via solve_dielectric_modes (Phase 1B).",
  "Oracle is the EXACT scalar LP-mode characteristic equation (geode_core::fiber_lp::fiber_lp_neff, Phase 2A) — the Bessel-function dispersion relation, a 6-digit analytic ground truth (validated against scipy).",
  "WINDOW-LIMITED n_eff: the guided window (n_clad, n_core) is only ~0.39% wide, so ANY in-window n_eff is automatically <=0.4% from the oracle. The n_eff agreement (see [fundamental].neff_rel_err_vs_oracle) is therefore a squeezed-window artifact and is NOT the real validation. The HEADLINE is the normalized b in [normalized] / [fundamental].",
  "Mode selection: for single-mode operation (V<2.405) the fiber supports exactly one LP family (LP01, a polarization-degenerate pair). The full-vector pencil's gradient nullspace is dispersed across the whole thin window, so the TOPMOST in-window eigenpair is a near-ceiling gradient-contaminated artifact (b~0.73, pinned within ~1e-4 of the derived physical-index ceiling), NOT the genuine LP01. solve_dielectric_modes rejects that near-ceiling cluster (a geometry-derived margin, NOT fitted to the oracle), so the reported fundamental is the genuine LP01 pair (b~0.50-0.54).",
  "HONEST b-accuracy limitation: the genuine LP01 lands at b~0.50-0.54 vs oracle b~0.458 (~10-17% off). A mesh/radius sweep (4.5k -> 26k edges) shows b PLATEAUING in 0.50-0.57 (it does not trend monotonically toward the oracle), a systematic first-order-Nedelec bias on this near-uniform-eps (kappa~0.008) weakly-guiding cross-section on the concentric-polar disk mesh - NOT under-resolution that refines away. The b tolerance ([normalized].b_tolerance) is set to the honestly-achievable value; we do NOT loosen arbitrarily or fit to the oracle.",
  "Single-mode: V < 2.405 (first zero of J0), so LP01 (no cutoff) guides and LP11 is below cutoff (the oracle returns None for LP11 here) — the defining property of single-mode telecom fiber.",
  "Open boundary: the weakly-guiding LP01 field has a long evanescent cladding tail; the computational radius is pushed to several core radii (measured in cladding decay lengths below) so the tail has decayed to ~0 at the PEC wall. The two radii differ by < 1e-3 in n_eff, confirming the truncation is immaterial.",
  "Genuine solver output — NOT fit to the oracle. First-order Nedelec on the concentric-polar disk mesh.",
]

[geometry]
core_radius_um = 4.1
n_core = 1.4504
n_clad = 1.4447
eps_core = 2.103660e0
eps_clad = 2.087158e0
numerical_aperture = 1.284604e-1
relative_index_difference = 3.922228e-3

[normalized]
# HEADLINE discriminator: normalized b stretches the 0.39%-wide window
# onto (0,1). b_fem vs b_oracle is the real, window-independent test.
v_number = 2.135017e0
v_single_mode_cutoff = 2.405
single_mode = true
b_oracle = 4.580943e-1
b_fem = 5.225914e-1
b_rel_err_vs_oracle = 1.407943e-1
b_tolerance = 2.000000e-1
within_b_tolerance = true
b_fem_trend = [5.378342e-1, 5.225914e-1]
b_converges_to_oracle = false  # plateaus ~0.50-0.57; see notes

[oracles.lp]
# Exact LP-mode characteristic equation (geode_core::fiber_lp, Phase 2A).
exact = true
n_eff_lp01 = 1.447313923904518e0
n_eff_lp11 = "none (below cutoff, V < 2.405)"
lp11_below_cutoff = true

[fundamental]
# Benchmark fundamental = the genuine LP01 (largest-radius run below).
# HEADLINE: b_fem vs b_oracle (see [normalized]). n_eff is reported
# only as WINDOW-LIMITED context (the 0.39%-wide window makes any
# in-window n_eff trivially <=0.4% from the oracle).
n_eff = 1.447681570475721e0
b_fem = 5.225914e-1
b_oracle = 4.580943e-1
in_window = true
# HEADLINE pass/fail (normalized b — the real discriminator):
b_rel_err_vs_oracle = 1.407943e-1
b_tolerance = 2.000000e-1
within_b_tolerance = true
# CONTEXT pass/fail (n_eff — window-limited, NOT the real validation):
neff_rel_err_vs_oracle = 2.540199e-4
neff_context_tolerance = 1.000000e-2
within_neff_context_tolerance = true
cluster_spread = 2.099733e-3

[open_boundary]
# Two computational radii; n_eff change must be below the threshold
# (evanescent tail decayed -> PEC truncation immaterial).
n_eff_radius_0 = 1.447768444237385e0
n_eff_radius_1 = 1.447681570475721e0
n_eff_radius_delta = 8.687376e-5
convergence_threshold = 1.000000e-3
converged = true

[radius_0]
radius_mult = 8.000
outer_um = 3.280000e1
outer_decay_lengths = 1.252609e1
n_radial = 8
n_angular = 96
n_edges = 4512
n_eff = 1.447768444237385e0
b_fem = 5.378342e-1
neff_rel_err_vs_oracle = 3.140441e-4
b_rel_err_vs_oracle = 1.740688e-1
n_eff_all = [1.447768e0, 1.447768e0, 1.447143e0, 1.446172e0, 1.446006e0, 1.445775e0]
solve_s = 1.825

[radius_1]
radius_mult = 11.000
outer_um = 4.510000e1
outer_decay_lengths = 1.697755e1
n_radial = 10
n_angular = 120
n_edges = 7080
n_eff = 1.447681570475721e0
b_fem = 5.225914e-1
neff_rel_err_vs_oracle = 2.540199e-4
b_rel_err_vs_oracle = 1.407943e-1
n_eff_all = [1.447682e0, 1.447682e0, 1.447028e0, 1.446019e0, 1.445834e0, 1.445582e0]
solve_s = 4.278

