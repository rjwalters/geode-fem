# Scoring — conformal-antenna-diffopt.1

Rubric: `anvil-pub-v2` (/44, advance ≥35). Every quoted span is byte-verbatim from `main.tex` (single-file body; the two `\include`-looking hits are `\includegraphics`, not `\input`/`\include`).

| # | Dimension | Weight | Score | Justification |
|---|---|---|---|---|
| 1 | Rigor of method / argument | 6 | 5 | The forward pencil, the complex-symmetry argument for factorization reuse, and the AD-kernel construction are sound and adequate for the reachability claim: the transpose adjoint "uses the \emph{same} operator as the forward solve ... $n_{\text{fact}}=1$ per frequency" (§3.2), and correctness is pinned by an FD gate at "relative error $1.17\times10^{-10}$" (§4). Docked one point: the adjoint identity $\lambda^{\top}(\partial A/\partial X)\mathbf{x}$ is asserted rather than derived, and the whole argument rests on "a single fixture at one bend radius and one band" (§5), so method rigor is high but argument breadth is thin. |
| 2 | Evidence sufficiency | 6 | 3 | The reachability result is well-validated (determinism, guards, cross-check), but the evidence base is one demonstration: "The reported design is a single fixture at one bend radius and one band; we have not swept mesh resolution, bend radius, or band placement" (§5). No baseline and no ablation isolate the load-bearing design choices (no low-DOF-vs-73-DOF or with/without-harmonic-morph comparison), and the title's structured-grid contrast is unmeasured — "The comparative superiority over structured-grid density methods is \emph{not} yet quantified" (§5). Multiple gaps for a top venue. |
| 3 | Clarity of contribution | 5 | 5 | The contribution is extractable in one sentence per item from the numbered bullet list, and the scope is stated unambiguously: "The contribution is a specific, previously-unoccupied \emph{combination}" (§1) with the four contributions itemized immediately after. No instance of an unextractable or hedged-away contribution found. |
| 4 | Related-work positioning | 5 | 5 | Exemplary, honest positioning that engages the closest prior work on its actual merits and explicitly disclaims a first-adjoint claim: "this is explicitly \emph{not} a claim of ``first shape adjoint in EM.''" (§1), with wang2011 / ghassemi2013 / ham2020 each distinguished precisely ("in 2-D, time-domain, lossless, PEC-walled, with \emph{no PML, no ports, and no S-parameters}" — §2) and the white space stated with three named differentiators (§2.4). |
| 5 | Reproducibility | 5 | 4 | Strong: the artifact of record, all hyperparameters, and determinism are named — "All values from the committed artifact \texttt{conformal\_results.toml}" (§4 Fig.2 caption), "FD step $10^{-6}$, tolerance $5\times10^{-3}$" (§4), "the run is bit-identical on re-run" (§4). Docked one point: the rendered body never states the repository, branch, or commit (that provenance lives only in the `.tex` header comment), and the artifact currently resides on an unmerged feature branch, so a reader of the PDF alone cannot locate it. |
| 6 | Figure & table quality | 4 | 2 | Captions are self-contained and the results table is clean (booktabs, meaningful headers, correct alignment). But both figures are unrendered placeholders — the source tree holds only `figures/src/`, and each caption ends "(Placeholder --- rendered by \texttt{paper-figures} from \texttt{figures/src/}.)" (§3, §4), so `\includegraphics{figures/setup_schematic}` and `{figures/s11_band}` resolve to nothing and legibility/axis-labels/palette cannot be assessed. |
| 7 | Prose & structural quality | 4 | 4 | Abstract → intro → related → method → results → discussion → planned → conclusion flow is intact, tense/voice consistent, and cross-references resolve (`\ref{sec:planned}` etc. all have matching `\label`s). Prose is concise with no hand-waving — the honesty note is kept to a sentence: "an initial optimization run mis-recorded a premature (step-cap) stop as a physics limit; a corrected line search reached the true \texttt{target\_reached} terminal" (§5). Overfull-hbox/compile hygiene is unverifiable pre-audit (no PDF) and is not penalized. |
| 8 | Citation hygiene | 5 | 3 | Every `\cite{}` resolves to a `refs.bib` entry (no `[??]` risk) and every quantitative claim traces to `conformal_results.toml` (verified: −5.51/−12.06 dB, rel-err 1.17e-10, 73 DOFs, ×5.56, the full per-frequency table). Docked two points: the load-bearing prior-work entry `wang2011` carries a garbled/conflated title — "Shape Sensitivity Analysis for the Compressible {N}avier--{S}tokes Equations via Discontinuous {G}alerkin Methods and Adjoint-Based Shape Optimization for Electromagnetic Problems" (refs.bib) reads as two titles merged; `ghassemi2013` lacks volume/number/pages; and the "prior single-DOF capstone ... at FD relative error $\sim\!10^{-9}$" (§4) traces to no cited artifact. |
| 9 | Rhetorical economy | 4 | 3 | Tight overall and the contribution lands in under 90 seconds, but the "narrow, previously-unoccupied combination" thesis and its three differentiators are restated in nearly every section — abstract, intro, and again "The three stacked differentiators versus the prior art are: (1) freeform / high-DOF ...; (2) full open-radiator physics ...; (3) differentiable-by-construction" (§2.4), then once more in §6 — which a busy reviewer would find repetitive. |
| | **Total** | **44** | **34** | Below the ≥35 advance threshold. |
