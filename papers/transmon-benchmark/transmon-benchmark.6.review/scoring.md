# Scoring — transmon-benchmark.6 (rubric: anvil-pub-v2, /44, threshold ≥35)

Scored against `.anvil/skills/pub/rubric.md` (9 dimensions). Every justification
carries a verbatim quote from `main.tex` with a location anchor per the
quoted-evidence contract (issues #464/#475). The document is single-file (no
`\input`/`\include` children; `ResolvedTex.missing` is empty by construction).

Review method note: v6 is a sanctioned surgical revision of v5 (41/44,
`advance: true`, zero review flags; v6 exists to clear the v5 audit's one
critical flag). The reviewer diffed `transmon-benchmark.6/main.tex` against
`transmon-benchmark.5/main.tex`, confirmed the diff contains exactly the four
changelog-declared changes plus the header provenance comment (no other file
in the version dir differs except `changelog.md`, `_progress.json`, and the
recompiled `main.pdf`), re-verified each change against its committed
artifact, and carried v5 dimension scores where the diff does not touch the
scored material — re-deriving each quoted justification from the v6 body.

| # | Dimension | Weight | Score | Justification |
|---|---|---|---|---|
| 1 | Rigor of method / argument | 6 | 6 | Untouched by the v6 diff; carried from v5 on re-verification. The discrete-adjoint layer is derived from the standard identity and every gradient is "validated against central finite differences of the full pipeline, with mutation tests" (— Table 2 caption, §8.1) rejecting sign, conjugation, and dropped-term errors; the variational-stationarity collapse is verified numerically ("confirming the collapse numerically rather than assuming it" — §8.2); the oracle-first tripwire protocol and the two-tier honesty structure (affine loop proof disclosed as such, real-mesh demo with a published boundary) carry over intact. |
| 2 | Evidence sufficiency | 6 | 5 | Untouched by the v6 diff; carried from v5. The 2×2 matrix, the chain, and both demo tiers carry committed FD-validated evidence, but the optimization evidence for the headline capability remains deliberately narrow: "The optimization demonstrations are single-parameter by design; nothing here demonstrates multi-parameter or constrained optimization behavior." (— §12, Threats to validity); the real-device convergence is a within-budget 0.5%-move target and the driven column is validated at operator level only — honestly bounded, but a PC member would still ask for one multi-parameter or RF-observable exercise before full weight. |
| 3 | Clarity of contribution | 5 | 5 | Untouched by the v6 diff; carried from v5. Five contributions are bulleted one sentence per item on the first pages; the ~250-word abstract leads with the inverse-design gap, states the capability, the honest negatives, the credential, and the boundary — "The contribution is a capability, not a speed race" (— Abstract) is extractable on triage, and the LOM-vs-eigenmode scope is stated in both the abstract and the contribution list. |
| 4 | Related-work positioning | 5 | 5 | Untouched by the v6 diff (the §1 unquoting does not alter the positioning claim, which remains accurate against the QDO abstract); carried from v5. Substrate-backed per the `.4.litsearch` sibling (8 resolver-verified candidates, all merged or explicitly declined with reasons): QDO, the SQcircuit line, SQuADDS, the photonics genus anchor, JAX-FEM at CPC 2023, TensorGalerkin, and the libCEED foil are engaged on the merits; the wedge stays hedged — "That wedge is, to our knowledge, uncontested; this paper occupies it." (— §2). No close prior work for the reframed claim is ignored. |
| 5 | Reproducibility | 5 | 5 | Carried from v5 and slightly strengthened by the v6 diff: "Every artifact behind every number in this paper is committed and scripted." (— §12) — SHA-256-pinned mesh, committed TOMLs/logs for every cell, per-cell hardware+commit provenance including the 128 GB-box disclosure; the new Table 3 footnote closes the audit's last traceability gap by disclosing that "the off-target Peak RSS cells repeat the physical-target measurements of the same session" (— Table 3 footnote, §11.1), verified this pass against `[matched.off_target.*]` in the committed TOML (wall times only, exactly as stated). Repo URL/DOI remains an operator-gated TODO (submission blocker, not scored down per thread convention). |
| 6 | Figure & table quality | 4 | 4 | Carried from v5; the one v6 table change is an improvement: Table 3's off-target Peak RSS cells now carry a ddag provenance footnote ("the off-target Peak RSS cells repeat the physical-target measurements of the same session" — Table 3 footnote) so the caption's artifact sourcing is no longer an overclaim; both large-scale geode rows with the dagger protocol note, the two-panel Figure 5 from the committed diffopt TOMLs, and the matched-cell fig4 inset are unchanged; captions re-verified self-contained against the freshly compiled PDF. |
| 7 | Prose & structural quality | 4 | 4 | Structure and voice unchanged by the surgical diff ("Performance is context here, not contribution" — §11); the reviewer compiled the v6 source to a clean fixpoint (4 pdflatex passes + bibtex; pass 3 still emitted the rerun warning, pass 4 clean): zero undefined citations/references, a single 1.13 pt overfull hbox (below the 5 pt gate), 25 pages, and the shipped `main.pdf` is current (pdftotext stream identical to the fresh compile). The new §9.2 clarifier parenthetical reads cleanly and resolves the anchor-proximity ambiguity. |
| 8 | Citation hygiene | 5 | 5 | The v5 deduction is repaired exactly as prescribed: §1 now paraphrases without quotation marks — QDesignOptimizer "iterates time-consuming electromagnetic simulations of HFSS-class solvers" (— §1) — no longer asserting verbatim source words, and the claim substance remains supported by the cited abstract (per the v5 audit's live-arXiv verification of 2508.18027); the remaining quote spans in the body are the solver's own name, a table-column name, and two scare quotes on the paper's own words — no instance of an asserted cited-source quotation found. All 57 cite keys resolve to complete entries (zero missing, zero unused; clean bibtex pass this compile), and the 0.2155/0.2156 anchor proximity now carries the clarifier ("both values are artifact-faithful" — §9.2), verified against both committed TOMLs. |
| 9 | Rhetorical economy | 4 | 3 | Untouched by the v6 diff (the two added clarifiers are justified and add ~10 lines); carried from v5. The paper remains 25 compiled single-column pages against the BRIEF's ~15-single-column-equivalent target, and the conclusion still re-walks the abstract and contribution list nearly item for item ("Around the capability we kept the honest ledger" — §14); the changelog's declination of the compression levers for the final surgical pass is reasonable, but the deduction stands on the artifact as shipped. |
| | **Total** | **44** | **42** | |
