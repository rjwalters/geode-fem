# Changelog — transmon-benchmark.6 (revised from transmon-benchmark.5)

Surgical, minimal-diff revision (iteration 6 of max 6). v5 scored 41/44
with `advance: true`; per pub-revise step 4, revision is required only
because `transmon-benchmark.5.audit/flags.md` carries one critical flag.
This pass addresses that flag, the audit's two traceability notes, and
the trivial reviewer nits — nothing else. Total diff vs
`transmon-benchmark.5/main.tex`: 44 changed lines (15 of them the
header provenance comment). No sections, numbers, citations, or figures
were otherwise touched.

| Source | Note | Resolution |
|---|---|---|
| transmon-benchmark.5.audit (critical-flag) | §1 quotation of `eriksson2025automated` ("time-consuming electromagnetic simulations") is not verbatim — the source abstract reads "time-consuming **iterative** electromagnetic simulations requiring manual intervention" | Removed the quotation marks and kept the (accurate, attributed) paraphrase: "iterates time-consuming electromagnetic simulations of HFSS-class solvers" (§1). Chosen over restoring "iterative" inside quotes because "iterates … iterative" reads badly and the abstract (L71–72) already paraphrases the same source unquoted; this makes the body match the v5 changelog's stated intent ("phrasing paraphrases their abstract"). Verified no other quotation in the paper asserts cited-source words: the remaining `` spans are the solver's own name ("geode-fem"), a table-column name ("FD rel-err"), and two scare quotes on our own words ("peak", "never completes"). |
| transmon-benchmark.5.review (generic, major) | Same defect, independently flagged (D8, priority 1): quote verbatim or paraphrase | Same resolution as above (the review explicitly blessed the unquote-to-paraphrase option). |
| transmon-benchmark.5.audit (note) | Off-target Peak RSS cells in Table 3 (geode-fem 3.1 GB, Palace 0.5 GB/rank) have no counterpart in the committed `[matched.off_target.*]` tables, which record wall times only | Kept the four cells but annotated provenance honestly: added a `\ddag` marker to each off-target Peak RSS cell and a table footnote stating that the committed `[matched.off_target.*]` tables record wall times only and the off-target RSS cells repeat the physical-target measurements (same session, same 133,108-DOF pencil) and are not independently recorded in the artifact. Chosen over dropping the cells (smallest honest diff; the values remain informative and the footnote removes the traceability overclaim). |
| transmon-benchmark.5.audit (note) | "137×" (§8.3) computed from displayed medians; the artifact's own HONEST READ comment computes 136× from unrounded medians (4.388126/0.032302 = 135.9) | Aligned to the artifact: "Computed from the artifact's unrounded medians, the assembled-CSR COCG on CPU is 136× faster at 1,854 edges (displayed: 0.032 versus 4.39 s)". The companion ratios in the same sentence (44×, 13.5×) are unchanged — both agree with the artifact comment under either basis. |
| transmon-benchmark.5.review (generic, minor) | 0.2155-vs-0.2156 GHz anchor proximity (§9.2 vs §9.1/§6) can read as a typo | Added the suggested half-sentence clarifier in §9.2: the last digit differs because the pad run targets the rounded 89.9 fF (`pad_results.toml` `e_c_target_ghz = 0.215464`) rather than the back-solved 89.843 fF (`results.toml` `e_c_target_ghz = 0.215600`); both artifact-faithful. |
| transmon-benchmark.5.review (nit) | `fig4-cpu-wallclock.pdf` renders as Figure 6 and `fig6-participation.pdf` as Figure 4; rename at a convenient pass | Declined for this pass — not a pure rename: the figure-source scripts hardcode their output filenames (`OUT = HERE.parent / "fig4-cpu-wallclock.pdf"` etc.) and document them in docstrings, so the swap requires editing script internals and re-rendering. Reviewer already classed it as cosmetic with "no reader-visible defect"; too risky for the last allowed iteration. |
| transmon-benchmark.5.review (nit) | Fig. 6 inset bar labels round to integers vs caption's 28.7 core-s | Declined — reviewer notes it is consistent with the stated displayed-value convention; changing it requires a figure re-render (script edit), out of scope for the surgical pass. |
| transmon-benchmark.5.review (minor) | 25 compiled pages vs the BRIEF's target; compression levers listed | Declined for this pass — any of the listed levers (conclusion rewrite, §6.2 restructure) is a restructuring edit, explicitly out of scope for the iteration-6 surgical pass; the venue two-column layout (lever 1) is an operator/venue decision. |
| transmon-benchmark.5.review (minor) | Five `TODO(operator)` markers are hard arXiv-submission blockers | Untouched by convention — operator-owned inputs (title wording, affiliations, cubecl f64 tracking URL, repo URL/DOI, acknowledgment); carried forward verbatim. |
| transmon-benchmark.5.audit (note) | 49 citations unverifiable on-disk (no PDFs in `refs/`); uncitable HFSS/COMSOL architectural claim (hedged) | No action available to the reviser — noted for author awareness per the audit; the hedge ("to our knowledge") is already in place. |
