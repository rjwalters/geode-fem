# Findings — transmon-benchmark.1 (cross-section observations)

1. **The single systemic defect is stale-brief inheritance, not fabrication.** Every blocker traces to one cause: the draft used the brief's older agreement numbers (0.029% worst; 4-decimal geode values; 20.6976/26.0809 in the geode column) while the BRIEF was subsequently corrected to the committed `benchmarks/transmon_eigen/results.toml` (worst 0.032%; 3-decimal geode values; junction 0.001%). The numbers the draft carries are internally consistent with each other — which is why the paragraph-local numeric detector passes — but disagree with the artifact the paper itself names as its data source (Fig. 2 caption, §9). The fix is mechanical and enumerated per-site in `comments.md`; no argument structure changes.

2. **The honest-science register is executed, not just claimed.** Both mandated caveats appear in the abstract; the absent qubit mode and the spurious mode each get a full section framed as findings rather than apologies; the performance section explicitly refuses a ranking ("an architecture trade-off, not a ranking") and refuses to report the degraded 16-rank configuration as data. This satisfies the brief's voice rules and materially supports dims 1, 7, and 9.

3. **The GPU section is structurally sound as a placeholder cell.** Scope, precision qualification (f32-only burn-cuda 0.21), the driven-solve-vs-eigensolve distinction, and the Palace-GPU contingency are all stated in prose with unmistakable `\TBDGPU` markers where numbers will land; the fig5 slot is deliberately a comment, not a figure environment. No fabricated GPU number exists anywhere in the body (verified by scan). Judged on structure per the BRIEF's mandate.

4. **Related-work positioning is load-bearing and lands.** The two November-2025 Palace-workflow papers are distinguished on their actual merits (wrapped-and-trusted vs. independently *implemented*; residuals mixing meshing/model-entry vs. same-mesh isolation), and the paragraph converts the litsearch absence-trail into a defensible novelty claim ("first independent third-party validation of Palace itself") without overreach. The single related-work gap (tree–cotree citation) was pre-identified by the litsearch sibling and is tagged in `comments.md`.

5. **Table 2 is the weakest artifact in an otherwise strong draft.** It is the paper's headline evidence, and it is both structurally lossy (collapsed cell) and the epicenter of the numerical inconsistency. Rebuilding it as six per-mode rows straight from results.toml resolves the critical flag, most of dim 6's deduction, and makes the eventual pub-audit numbers-vs-artifact pass mechanical.

6. **Pre-flight status this pass**: render gate skipped (audit-first fail-open; audit critic in parallel); numeric-consistency detector pass (sidecar at `transmon-benchmark.1.numeric/`); corpus provenance tier inactive; subject voice tier inactive; no venue overlay (no thread `.anvil.json`); no `artifact_verify` declaration.

<!-- Rubric version transition: omitted — this is the first review iteration for this thread (no prior review sibling exists; transmon-benchmark.0 was litsearch-only), so there is no prior rubric to transition from. -->
