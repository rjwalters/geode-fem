# Scoring — transmon-benchmark.1 (anvil-pub-v2, /44)

Reviewed: `transmon-benchmark.1/main.tex` (single-file thread; `resolve_tex_inputs` found no `\input`/`\include` children, `ResolvedTex.missing` empty) + `refs.bib` + `figures/src/` + litsearch sibling `transmon-benchmark.0.litsearch/`.

| # | Dimension | Weight | Score | Justification |
|---|---|---|---|---|
| 1 | Rigor of method / argument | 6 | 6 | The validation protocol is exemplary: "every claim is anchored either to an exact analytic quantity or to a committed reference artifact" (— §5), with same-mesh/same-physics control, two analytic tripwires ($f_{LC}$ anchor; $1/\sqrt{2}$ L-scaling), inverse tests that must fail, participation-based mode identification, and a bit-for-bit oracle determinism check. The per-core efficiency claim is derived soundly (1 core vs 4 ranks at equal wall time) and explicitly framed as an architecture trade-off. |
| 2 | Evidence sufficiency | 6 | 5 | Six-mode agreement on a real production geometry plus a timed CPU cell is adequate for the scoped claims, and the paper self-limits honestly: "One geometry, one mesh, first order only" (— §10 Limitations) and "Performance numbers are $n = 3$ full-pipeline measurements on one instance type" (— §10). One point off: a single geometry/order and a pending GPU cell leave the methodology-generality claim resting on one instantiation; a second geometry or a mesh-refinement point would close the gap. |
| 3 | Clarity of contribution | 5 | 5 | Four bolded contribution bullets in §1 are each extractable in one sentence, led by "The first independent validation of the Palace/DeviceLayout.jl transmon stack." (— §1); the abstract states the claim, the mechanism, and both caveats in order. |
| 4 | Related-work positioning | 5 | 5 | Substrate-backed per the litsearch sibling (all cited candidates resolver-verified; absence-of-artifact trail documented): §2 engages the two closest works (sommers2025open, ye2025electromagnetic) head-on with their actual agreement levels, and states the wedge precisely — "Our study eliminates meshing and model entry as confounders" (— §2). The TEAM tradition is handled honestly in prose with a footnote explaining the DOI-less record. Dim 4 = 5/5 on substrate-grounded positioning per rubric §"Perspective substrate". |
| 5 | Reproducibility | 5 | 4 | Commits, SHA-256-pinned fixture, committed configs/oracles, hardware and versions are all disclosed, and the MSH 2.2 conversion gotcha is documented. Two gaps: the repo locator is still pending — "Repository URL and archival DOI for the geode-fem source at commit" (— §9 fn.) — and the BRIEF-mandated precision disclosure (geode values stored at 3 decimals in results.toml; rel_err computed against rounded values; full-precision agreement ~0.029% worst) is absent from §9. |
| 6 | Figure & table quality | 4 | 2 | The headline agreement table is structurally lossy and numerically stale: it collapses four modes into one cell — "15.4650 / 18.6927 / 20.6976 / 26.0809" (— Table 2) — discarding per-mode Palace values and per-mode deviations, and those values do not match the committed results.toml (see critical flag). Captions elsewhere are self-contained and strong (Fig. 2's caption names its data artifacts); figures are unrendered `\anvilfig` placeholders pending pub-figures, so print-size legibility is unassessable this pass. |
| 7 | Prose & structural quality | 4 | 4 | Flow is intact abstract→intro→methods→results→discussion, the register matches the brief's honest-science voice — "an architecture trade-off, not a ranking" (— Abstract) — tense/voice are consistent, all `\ref`/`\cite` targets exist (mechanically verified), and main.pdf compiled at draft time. No hand-waving found. |
| 8 | Citation hygiene | 5 | 4 | All 28 `\cite` keys resolve to refs.bib entries (mechanically verified; zero orphans either direction) and non-trivial claims are cited. Deductions: the future-work claim "a divergence-free or tree--cotree gauge projection on geode-fem's eigenpath" (— §8) is uncited (a gap the litsearch sibling explicitly named); `logg2010dolfin` carries the truncated title "DOLFIN"; `oberkampf2010verification` is typed `@article` with no journal field (it is a CUP monograph — should be `@book` with publisher). |
| 9 | Rhetorical economy | 4 | 4 | Claims are surfaced early ("Two caveats are stated up front" — Abstract), every section is load-bearing, hedging is proportional to genuine uncertainty (the GPU section states scope precisely so "the eventual numbers cannot be over-read"), and the paper fits the 8–12 page target without restatement padding. |
| | **Total** | **44** | **39** | Threshold ≥35 met, but one critical flag (numerical inconsistency vs the committed source-of-truth artifact) short-circuits: **advance: false**. |
