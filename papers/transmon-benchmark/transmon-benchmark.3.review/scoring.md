# Scoring — transmon-benchmark.3

Rubric: `anvil-pub-v2` (/44, advance ≥35). Scored against `main.tex` (single-file
paper; no `\input`/`\include` section children — `\includegraphics` only).

| # | Dimension | Weight | Score | Justification |
|---|---|---|---|---|
| 1 | Rigor of method / argument | 6 | 6 | The same-mesh, same-physics, same-junction-model protocol is methodologically airtight: geometry "meshed \emph{once} and both solvers consume the identical mesh, first-order discretization, and lumped reactive-shunt junction" (§1), so residuals isolate formulation/solver correctness rather than meshing luck. Independence is real — "a batched-tensor assembly feeding a direct-factorization Lanczos in Rust, and a projected, preconditioned Krylov method in C++" (§6) — and the analytic tripwires exclude self-consistent-but-wrong agreement. The gauge/projection arc (§9) is a genuine derivation with correct linear algebra. No rigor gap warrants a deduction. |
| 2 | Evidence sufficiency | 6 | 5 | Evidence is strong and multi-pronged: six-mode agreement table, L-scaling tripwire, participation-based mode ID, a determinism check ("rerunning Palace from the committed configuration reproduced its committed \texttt{eig.csv} bit-for-bit." — §5), a CPU cell with $n=3$, and a full 16-cell GPU scaling study. The defensible gap keeping this off ceiling is scope breadth for the general architecture thesis: the correctness claim rests on "One geometry, one mesh, first order only" (§Discussion) — a single geometry cannot fully substantiate the general claim, and the paper leans on the repo's other benchmarks only by reference, not in-paper. Honestly disclosed, but a real evidence boundary. |
| 3 | Clarity of contribution | 5 | 5 | The five contributions are enumerated as a labelled list, each extractable in one sentence (§1): "An architecture for FEM as a tensor-compiler workload." and "Honest-negative reporting as a first-class result." among them. The abstract opens with the reframed thesis — "We ask whether that investment can be inherited by classical mesh-based electromagnetics, and answer with a measured, honestly bounded yes." — so the tensor-compiler identity is unambiguous from the first paragraph, confirming this is an executed reframe rather than a relabel. |
| 4 | Related-work positioning | 5 | 5 | §2 is substrate-backed (litsearch sibling present) and defends the wedge on all three qualifiers head-on: it names the libCEED foil — "\emph{Palace itself runs on that stack}" — and distinguishes the concurrent TensorGalerkin directly ("TensorMesh is nodal rather than $H(\mathrm{curl})$-conforming, its electromagnetic examples are magnetostatic rather than full-wave"). It engages the two closest Palace-workflow papers on their merits ("Both efforts treat Palace as a component to be \emph{wrapped and trusted}"). Dim 4 = 5/5: closest-prior-work cluster cited with explicit extends/contradicts framing, substrate-backed per the litsearch sibling. |
| 5 | Reproducibility | 5 | 5 | §11 is exhaustive: SHA-256-pinned MSH fixture, committed fixture generator, committed Palace config, and the MSH conversion gotcha disclosed ("MFEM's MSH~4.1 reader rejects the DeviceLayout multi-entity-block file"). The precision-of-committed-numbers disclosure — "the committed per-mode \texttt{rel\_err\_pct} is computed against the rounded geode-fem values" — plus per-benchmark `results.toml` paths, exact EC2 hardware, and pinned commits for both solvers give a replication group everything needed. |
| 6 | Figure & table quality | 4 | 4 | All five figures resolve to real committed PDFs and captions are self-contained: Fig. 6's caption alone tells the story — "geode-fem's un-projected headline Lanczos additionally leaks one spurious junction-localized mode at $3.4528$\,GHz ($p = 0.9942$) with no Palace counterpart". Tables carry meaningful headers and units, and the GPU-cell caption honestly flags the f32 ceiling ("run-to-run convergence is nondeterministic at that size (f32 floor)"). |
| 7 | Prose & structural quality | 4 | 4 | Flow is intact (abstract → intro → related → architecture → problem → methods → results → performance → repro → discussion → conclusion); prose is concise and hand-waving-free — the honest-negative register is executed consistently ("we prefer publishing the bound to implying the win" — §12). No broken cross-references survive in the rendered PDF (pdftotext shows zero `Section ??`/`[??]` tokens). Tense and voice are consistent; the operator-gated TODO markers are content notes, not prose defects. |
| 8 | Citation hygiene | 5 | 5 | Every non-trivial claim carries a citation and the load-bearing concurrent-work is engaged in prose, not just listed: "Closest to this work is the concurrent TensorMesh/TensorGalerkin framework \citep{wen2026learning}" (§2). Out-of-band verification confirms all 50 `\cite{}` keys resolve to a `refs.bib` entry (zero cited-but-missing), the rendered PDF carries no `[??]` unresolved-citation token, and the concurrent-work entries carry full arXiv-ID metadata. The physics framing is anchored — "resonating against the \emph{pad (shunt) capacitance} of roughly $80$--$100$\,fF \citep{koch2007}" (§7). Claim-support is the auditor's job; the hygiene half is clean. |
| 9 | Rhetorical economy | 4 | 3 | The paper earns much of its 23 pages: the expanded architecture section, the gauge/projection arc, and the four-config GPU study are load-bearing. But at 23 pages against a BRIEF target of 8-12 two-column (~15 single-column) it exceeds the operator's own bound, and passages restate rather than advance: the abstract already carries the full arc verbatim ("a port-aware rank-1 re-admission that lands all six modes at $\leq 0.029\%$") which §9 then re-derives at length, and the two Discussion paragraphs "What the cross-validation does and does not establish." / "What the performance evidence supports." re-state conclusions already made in §7 and §12's honest-read. A busy reviewer can still extract the contribution in 90 seconds, so this is a trim-not-restructure deduction. |
| | **Total** | **44** | **42** | |
