# Scoring — transmon-benchmark.4 (rubric: anvil-pub-v2, /44, threshold ≥35)

Scored against `.anvil/skills/pub/rubric.md` (9 dimensions). Every justification
carries a verbatim quote from `main.tex` with a location anchor per the
quoted-evidence contract (issues #464/#475). The document is single-file (no
`\input`/`\include` children; `ResolvedTex.missing` is empty by construction).

| # | Dimension | Weight | Score | Justification |
|---|---|---|---|---|
| 1 | Rigor of method / argument | 6 | 6 | The methodology is exemplary for a cross-solver benchmark: the thesis is explicitly held to "same-mesh, same-physics cross-validation against the reference solver, at production accuracy, with every number traced to a committed artifact" (— §1), with analytic tripwires ($f_{LC}$, the $1/\sqrt{2}$ L-doubling law), inverse tests that must fail, a matched same-session CPU cell (both solvers, 1 and 8 cores, three workloads), and a measured three-step gauge/projection arc that upgrades the spurious-mode story from disclosure to diagnosis. The v4 matched-cell migration strengthened rigor over v3. |
| 2 | Evidence sufficiency | 6 | 5 | The agreement and CPU cells are strong, but the GPU evidence remains off the flagship problem — "This cell is a driven-solve scaling study on a cube fixture family, not the transmon eigenproblem" (— §10.3) — and the study is "One geometry, one mesh, first order only" (— §12, Limitations), so the architecture claim rests on a single validation vehicle. Same one-point deduction as v3; unchanged by the number-migration revise. |
| 3 | Clarity of contribution | 5 | 5 | The five contributions are bulleted, one sentence per item, extractable from the abstract and first page — e.g. "Evidence that a general-purpose ML tensor stack can carry production-accuracy computational electromagnetics." (— §1, contributions). The abstract states wins, losses, and honest negatives in order. (Note: under the 2026-07-16 BRIEF reframe this contribution set is superseded for v5, but v4 is scored as it stands.) |
| 4 | Related-work positioning | 5 | 5 | Substrate-backed per the litsearch siblings (`.0.litsearch`, `.2.litsearch`): the two closest validation-axis works (SQDMetal, the Palace-workflow paper) are engaged on the merits ("reporting agreement at roughly the $0.02$--$0.13\%$ level" — §2) and distinguished as not-same-mesh; the concurrent TensorGalerkin work is cited and sharpened against; the libCEED foil is stated head-on ("Palace itself runs on that stack" — §2). No close prior work for v4's claims is ignored. Web-search leads for the v5 reframe axis are recorded in comments.md as `related-work` leads. |
| 5 | Reproducibility | 5 | 5 | "Every artifact behind every number in this paper is committed and scripted." (— §11) — SHA-256-pinned mesh fixture, committed configs/oracles, bit-for-bit Palace determinism check, per-cell instance/driver/commit provenance, and a disclosed precision-of-committed-numbers note. The repository URL/DOI is an explicitly marked operator-gated TODO (submission blocker, not scored down per the established thread convention). |
| 6 | Figure & table quality | 4 | 3 | Captions are self-contained and artifact-pointed, but Table 3's large-scale block was left behind by the PR #557 correction: the caption states "at ${\sim}1.16$M DOFs the direct factorization is OOM-killed (63.9\,GB peak on the ${\sim}61$\,GB box) while Palace completes" (— Table 3 caption, §8.2) and the table carries only the OOM-killed row, while the body's corrected finding (completion at 565.5 s / 92.2 GB on a 128 GB box) appears nowhere in the table — the table now tells the superseded story. |
| 7 | Prose & structural quality | 4 | 3 | Flow and voice are consistent and hand-waving is absent, but two overfull hboxes above the 5 pt gate threshold persist in §10 (36.96 pt and 17.75 pt, source lines 991–1005 and 1008–1020, in the paragraph beginning "On 2026-07-14, geode-fem code executed on a physical GPU for the first time" — §10.2), and the in-place #557 edit left the abstract carrying a causal clause its own results section retracts, a structural seam a careful reader will catch. |
| 8 | Citation hygiene | 5 | 5 | All 50 `\cite{}` keys resolve to `refs.bib` entries (zero missing, zero unused; verified by key cross-check), the compile fixpoint shows zero undefined citations or references, and entries carry complete author/title/venue/year fields including the concurrent-work eprints; no instance of an unresolved or malformed citation found. |
| 9 | Rhetorical economy | 4 | 3 | At 24 compiled pages against the BRIEF's 8–12 two-column (~15 single-column) target, the v3 review's specific trims were not applied (v4 was a number-migration revise): the Discussion still restates §8/§10 conclusions — "The measured position is: per-core CPU competitiveness demonstrated" (— §12) re-walks the CPU-cell paragraph structure — and the single-paragraph abstract runs ~40 source lines. Same deduction as v3, now with the recommendation escalated since the v5 reframe must add content. |
| | **Total** | **44** | **40** | |
