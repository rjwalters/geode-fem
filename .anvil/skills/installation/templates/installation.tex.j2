%% installation.tex — Anvil installation-art proposal template
%%
%% Compile with: xelatex installation.tex   (NOT pdflatex — see anvil-installation.cls)
%%
%% Generated by anvil:installation. The double-brace placeholders are
%% substituted by the drafter from the BRIEF.md frontmatter. The 11 section
%% skeletons are
%% starting points; the drafter elaborates each into prose, tables, and figure
%% references. The section structure mirrors the canonical "Quiet Place" worked
%% instance (see examples/expected-thread.1/README.md).
%%
%% Frontmatter placeholders (all optional, defaults shown):
%%   title            — piece name
%%   subtitle         — one-line gloss
%%   studio           — author/studio name (footer + kicker)
%%   date             — proposal date
%%   stage            — e.g. "ART INSTALLATION --- CONCEPT STAGE"
%%   signature_color  — hex (no #), default B45309 (warm amber)
%%   hero             — path to hero render under figures/ (omit if none)
%%   participatory    — true|false. When false, the Consent + Safety sections
%%                      (6/7/8) are omitted (a non-participatory light or sound
%%                      installation has no participant interaction to govern).

\documentclass{anvil-installation}

%% Signature color override (default B45309 lives in the class).
\definecolor{accent}{HTML}{ {{- signature_color | default("B45309") -}} }
\definecolor{rule}{HTML}{ {{- signature_color | default("B45309") -}} }

\installationtitle{ {{- title | default("Installation Title") -}} }
\installationsubtitle{ {{- subtitle | default("") -}} }
\installationstudio{ {{- studio | default("Studio") -}} }
\installationdate{ {{- date | default("\\today") -}} }
\installationstage{ {{- stage | default("ART INSTALLATION --- CONCEPT STAGE") -}} }

\begin{document}

\maketitleblock

%% Hero image (full-width). Drop the render into figures/ and name it in the
%% brief's `hero:` key. Omit the key entirely for a no-hero proposal.
\herofigure{ {{- hero | default("") -}} }

%% ── 1. Premise ───────────────────────────────────────────────────────────────
%% One-paragraph thesis of the piece, in a callout. A reader should grasp what
%% the piece IS and what it ARGUES without any wall text. This is the anchor for
%% rubric dimension 1 (conceptual coherence).
\begin{callout}[title=Premise]
{{ premise | default("One paragraph: what the piece is, where the visitor stands, and the single argument it makes. Legible without a wall text.") }}
\end{callout}

%% ── 2. The Frame ─────────────────────────────────────────────────────────────
%% The conceptual / cultural / legal anchor that gives the piece its argument
%% (Quiet Place: Katz v. United States). Two to four paragraphs situating the
%% work in an idea, not just describing its form.
\section{The Frame}

{{ frame | default("The idea the piece is built around. Name the anchor (a text, a doctrine, a phenomenon, a tradition) and the tension it exposes. Several paragraphs.") }}

%% ── 3. The Visitor's Hour ────────────────────────────────────────────────────
%% The choreography of the encounter as a timeline, in a metricbox table.
%% This is the anchor for rubric dimension 4 (visitor experience / ritual).
\section{The Visitor's Hour}

{{ visitor_intro | default("One paragraph framing the arc of the encounter from threshold to exit.") }}

\begin{metricbox}
\begin{tabularx}{\textwidth}{@{} >{\bfseries}l X r @{}}
{{ visitor_timeline | default("Threshold & What happens at the entrance. & $\\sim$30 s \\\\\nApproach & The body of the experience. & $\\sim$3 min \\\\\nThe encounter & The central moment. & 60 s \\\\\nExit & How the visitor leaves and re-enters the world. & $\\sim$3 min") }}
\end{tabularx}
\end{metricbox}

%% ── 4. Architecture ──────────────────────────────────────────────────────────
%% Form, geometry, siting. \subsection-s for distinct elements; a spec metricbox
%% table; figure references. The anchor for rubric dimension 2 (spatial /
%% architectural resolution). The form must be DESIGNED, not just described.
\section{Architecture}

{{ architecture_intro | default("The principal form, its scale, and where it sits. Name the architectural lineage.") }}

\subsection{ {{- architecture_sub1_title | default("The interior") -}} }

{{ architecture_sub1 | default("Describe a distinct spatial element. Reference a figure if one exists.") }}

\begin{metricbox}
\begin{tabularx}{\textwidth}{@{} >{\bfseries}l X @{}}
{{ architecture_spec | default("Outer dimension & value \\\\\nCirculation width & value \\\\\nKey clearance & value") }}
\end{tabularx}
\end{metricbox}

%% ── 5. The Light / Sensory Language ──────────────────────────────────────────
%% The sensory communication layer (light, sound, material, scent, temperature).
%% A `description` list of the distinct sensory voices. The anchor for rubric
%% dimension 3 (sensory / material language) — decisions must be specific.
\section{ {{- sensory_section_title | default("The Light Language") -}} }

{{ sensory_intro | default("One paragraph naming the sensory medium the piece speaks in and why it is the right one.") }}

\begin{description}[style=nextline, leftmargin=0.6em, itemsep=0.5em]
{{ sensory_items | default("\\item[Primary voice.] The dominant sensory element and what it does over the encounter.\n\\item[Secondary voice.] A supporting sensory element used sparingly for state.") }}
\end{description}

%% ── 6. The Ritual Act / Participation Mechanic ───────────────────────────────
%% The participatory mechanic — the thing the visitor DOES (Quiet Place: the
%% shedding of devices). Generalize from "The Shedding" to the act this piece
%% asks of its visitor. Included only for participatory pieces.
{% if participatory | default(true) -%}
\section{ {{- ritual_section_title | default("The Ritual Act") -}} }

{{ ritual | default("The act the piece asks of the visitor: what they do, what they give up or take on, and why the act is the point rather than a logistic. Several paragraphs.") }}

{% endif -%}
%% ── 7. The Consent Structure ─────────────────────────────────────────────────
%% Volitional / consent design — how participation is chosen, how to decline,
%% how the piece treats consent as the moral center. Required when participants
%% interact with each other or the piece in a way that needs consent.
%% Included only for participatory pieces. Anchors rubric dimension 6 (ethics).
{% if participatory | default(true) -%}
\section{The Consent Structure}

{{ consent | default("How the visitor consents (and how they decline without penalty). Treat consent as design, not a waiver. Name the volitional acts and their sequence.") }}

{% endif -%}
%% ── 8. Safety Without Surveillance ───────────────────────────────────────────
%% Safety designed without violating the piece's promise (Quiet Place: safety
%% without recording the encounter). A safety table: passive sensing, failsafe,
%% staffing. Included only for participatory pieces. Also anchors dimension 6.
{% if participatory | default(true) -%}
\section{Safety Without Surveillance}

{{ safety_intro | default("One paragraph: the safety promise, and the constraint the piece's concept places on how safety may be achieved.") }}

\begin{tabularx}{\textwidth}{@{} >{\bfseries\raggedright\arraybackslash}p{2.4cm} X @{}}
\toprule
\textbf{System} & \textbf{Function} \\
\midrule
{{ safety_table | default("Passive sensing & Door state, occupancy, environmental limits. Reveals trouble without capturing the encounter. \\\\\nFailsafe & The fail-open condition for every hazard mode. \\\\\nStaffing & Who is on duty and what they can (and cannot) observe. \\\\") }}
\bottomrule
\end{tabularx}

{% endif -%}
%% ── 9. References & Lineage ───────────────────────────────────────────────────
%% Art-historical / architectural precedent in a tabularx table. The anchor for
%% rubric dimension 7 (references & lineage) — situate the work, don't be naive.
\section{References \& Lineage}

\begin{tabularx}{\textwidth}{@{} >{\bfseries}l X @{}}
{{ lineage_table | default("Anchor work or doctrine & Why it grounds the frame. \\\\\nArchitectural precedent & The formal lineage of the structure. \\\\\nArtist precedent & A practitioner whose work this is in conversation with.") }}
\end{tabularx}

%% ── 10. Budget & Operations ───────────────────────────────────────────────────
%% Capital metricbox, annual operating metricbox, throughput, funding model.
%% The anchor for rubric dimension 5 (fabrication & buildability) — credible
%% materials, methods, and budget ranges (planning ranges, not bids).
\section{Budget \& Operations}

{{ budget_intro | default("One paragraph: what dominates capital cost, what dominates operating cost, and the basis of the figures (planning ranges, not bids).") }}

\subsection{Capital}

\begin{metricbox}
\begin{tabularx}{\textwidth}{@{} X r @{}}
{{ capital_table | default("Primary fabrication & \\$value \\\\\nSystems and controls & \\$value \\\\\nEngineering and contingency & \\$value \\\\\n\\midrule\n\\textbf{Subtotal} & \\textbf{\\$range}") }}
\end{tabularx}
\end{metricbox}

\subsection{Annual operating cost}

\begin{metricbox}
\begin{tabularx}{\textwidth}{@{} X r @{}}
{{ operating_table | default("Staffing & \\$value \\\\\nMaintenance and calibration & \\$value \\\\\nUtilities and insurance & \\$value \\\\\n\\midrule\n\\textbf{Subtotal --- annual} & \\textbf{\\$range / year}") }}
\end{tabularx}
\end{metricbox}

\subsection{Throughput \& funding model}

{{ throughput_funding | default("Expected visitor throughput per hour/day/year, and the plausible commissioning paths (permanent commission, biennale, standalone institutional).") }}

%% ── 11. Open Decisions ────────────────────────────────────────────────────────
%% The unresolved choices, tracked honestly (the memo "assumptions to validate"
%% analogue). The anchor for rubric dimension 8 (open decisions).
\section{Open Decisions}

\begin{enumerate}
{{ open_decisions | default("\\item \\textbf{First open choice.} The decision, the options, and what each implies.\n\\item \\textbf{Second open choice.} The decision, the options, and what each implies.") }}
\end{enumerate}

\coda{ {{- coda | default("One sentence the visitor carries out with them.") -}} }

\end{document}
