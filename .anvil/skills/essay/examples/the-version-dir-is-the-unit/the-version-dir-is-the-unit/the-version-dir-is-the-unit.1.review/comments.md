# Line-level comments — the-version-dir-is-the-unit.1

Keyed to `the-version-dir-is-the-unit.md`. Severity: `nit` / `minor` /
`major` / `critical`. Scope: `voice` / `economy` / `argument` / `structure`.

- **¶1, "made the mistake every file-oriented programmer makes"** —
  `[nit][voice]` Strong concrete-failure open per STYLE_GUIDE §"Open inside a
  concrete moment." Keep.
- **¶3, "So I stopped editing files and started writing directories."** —
  `[nit][structure]` Single-sentence pivot doing real structural work;
  earns its line. Keep.
- **¶4, "This sounds like overhead until you have lived without it."** —
  `[minor][voice]` Brushes the edge of forestalling pushback (VALUES.md
  anti-stance: no hedges to forestall). It earns its place by naming the
  objection rather than cushioning against it, but watch the register.
- **¶6, "You only catch this by setting two frozen reviews side by side"** —
  `[minor][voice]` A hair more explanatory than the corpus register; compare
  `corpus/exemplar-on-iteration.md` ("read the draft like a person."). Drives
  the dim-2 deduction.
- **¶6 + ¶9, "the total went up, the thing that was broken did not move"** —
  `[minor][economy]` The score-rose / flaw-static shape appears twice.
  Collapse one to buy full dim-9 weight.
- **¶7, "state on disk is the only state I trust"** —
  `[major][argument]` In a published version, link the
  read-from-filesystem orchestration mechanism. Dim-7 `major`, never
  critical — a named entity a curious reader would want a URL for.
- **Close, "Build on the one that stays."** —
  `[nit][structure]` Short declarative landing; reverses the opening's
  "moving point" image. Exactly the STYLE_GUIDE close shape. Keep.
