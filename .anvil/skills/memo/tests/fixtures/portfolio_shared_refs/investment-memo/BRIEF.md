# BRIEF — investment-memo (fixture placeholder)

Placeholder brief. The fixture's `README.md` documents the rationale —
this file is here so the thread root has the shape of a real memo
thread (with `BRIEF.md` and `refs/` at the root), but the content is
not exercised by the resolver tests.
