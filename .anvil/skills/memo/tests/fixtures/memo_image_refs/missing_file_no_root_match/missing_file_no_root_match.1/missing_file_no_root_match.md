# Memo where a ref is missing and no same-basename file is at the root

![Cohort valuations](exhibits/fig_x.png)

This file does not exist anywhere — there is no `fig_x.png` at the version
root either. The diagnostic should mention the resolved path but should NOT
mention the cp-r footgun shape.
