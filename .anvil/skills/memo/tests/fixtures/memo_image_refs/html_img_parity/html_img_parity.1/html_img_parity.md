# HTML img parity — both syntaxes treated symmetrically

The first reference is an HTML `<img>` whose source is MISSING:

<img src="exhibits/fig_b.png" alt="Missing exhibit">

The second is a markdown image whose source is PRESENT:

![Present exhibit](exhibits/fig_c.png)
