# walk-to-highest v1

Stub memo body for the `latest_symlink` fixture. Test asserts only that
the resolver picks `walk-to-highest.3` (the highest-N version dir) when
no `.latest` of any shape exists.
