# pinned-symlink v2 (symlink target)

The operator-pinned `.latest` resolves here even though v3 exists.
Load-bearing AC from issue #288: an author can pin `.latest` to a
non-highest version intentionally.
