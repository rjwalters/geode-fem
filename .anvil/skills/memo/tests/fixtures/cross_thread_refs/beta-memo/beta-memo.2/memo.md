# Beta memo (v2) — placeholder body

Latest beta-memo version. The alpha-memo fixture's
`[[../beta-memo/beta-memo.latest/memo.md]]` reference resolves here via
the walk-to-highest fallback (no `.latest` symlink in this fixture).

## §2 Framing

This is the framing alpha-memo §1 cites. The resolver verifies that
this file exists at `<fixture>/beta-memo/beta-memo.2/memo.md`; the
actual prose is irrelevant to the test — only the on-disk presence is
the contract under issue #287.
