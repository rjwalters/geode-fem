# Scoring — botho-bridge-spec.2 (spec-review, prose/structure/consistency reviewer)

Rubric `anvil-spec-v1`, /44, advance ≥39. Integer scores, weight = per-dim max. `code_ref` tier ACTIVE (declared `bridge/**/*.rs`, resolved 35 files, `missing=false`); dim 1 scored by judgment against the resolved implementation, with the exhaustive spec↔code sweep deferred to the parallel `spec-audit`.

| # | Dimension | Weight | Score | Justification |
|---|-----------|--------|-------|---------------|
| 1 | Normative correctness | 7 | 7 | Every live claim holds of the resolved implementation and every target-state mechanism is explicitly marked and registered. Spot-checked against code: the divergence note "The production unwrap path (\texttt{bridge/service/src/bth\_scan.rs}) emits a recipient output with \emph{empty} cluster tags---i.e.\ factor-1---and even asserts this" (§Implementation Status) matches `bth_scan.rs:218` `debug_assert!(recipient_output.cluster_tags.is_empty())`; the distinct-signer claim "a repeated or equivocating signer MUST count once toward $t$ (verified in \texttt{release/bth.rs::validate\_release\_attestation})" (§Threshold) matches `release/bth.rs` distinct-signer enforcement; and "one wBTH base unit equals one picocredit equals one unit of native \BTH, so the peg carries no scaling factor" (§Motivation) matches `WrappedBTH.sol:90 DECIMALS=12`. The v1 dim-1 deduction (base-layer CT claim reading present-tense-adjacent without a row) is resolved: the claim is reworded to "the live chain records \emph{public} amounts, and the confidential-amounts transition is specified and tracked at the base layer (ADR~0006 ...; botho\#902), out of scope for this section's register" (§Privacy) — an explicit base-layer target cross-ref, no bridge-scope drift remaining. +1 from v1's 6. |
| 2 | Internal consistency | 6 | 6 | Mechanical half: the step-3b `check_constant_consistency_multi` gate returned `found=true`, 7 declarations (5 distinct: `wbth_decimals=12`, `bridge_threshold_floor=t_scp`, `ring_size=20`, `import_epoch_blocks=17280`, `import_factor_floor=1.5`; the latter two each re-declared once with identical value+unit), `violations: []`, `passed=true` — no value-mismatch, no unit-mismatch, no malformed marker. Judgment half: the peg invariant "\eqref{eq:bridge:peg}" is stated once and reused by reference in §Security ("\eqref{eq:bridge:peg} MUST hold within" tolerance — §Security); the factor-1/zero-demurrage predicate is consistent across §Peg, §Privacy, §Security. No unmarked prose-level drift. |
| 3 | Claim precision | 6 | 6 | RFC-2119 discipline is now present and disciplined: a conformance clause defines the keywords ("The key words \textbf{MUST}, \textbf{MUST NOT}, \textbf{SHALL} ... are to be interpreted as described in RFC~2119" — §Conformance keywords) and normative obligations are uppercased at their normative points (e.g. "a mint or release MUST NOT be authorized by fewer than $t$ distinct signatures" — §Threshold; "a non-factor-1 deposit MUST be rejected with an audit event and MUST never mint" — §Peg). Claims are falsifiable and anchored to code symbols. +1 from v1's 5 (the sole v1 deduction was the missing RFC-2119 clause). |
| 4 | Completeness | 5 | 5 | Every component the section normatively governs is covered — custody/threshold, per-chain schemes, attestation domains, exactly-once, three-Safe split, peg, privacy leakage, import tagging, five security invariants, and the six-row status register. No dangling "TBD" in a load-bearing predicate: "no instance of an undefined-but-referenced load-bearing entity found" — the domain-tag table, the epoch equations, and each IMP-row resolve their own referents. |
| 5 | Technical accuracy | 5 | 5 | Internal logic holds and the Sybil-resistance argument is airtight: "intra-epoch splitting piles into the same pool and still hits the high factor" while "Diluting requires spreading across \emph{epochs}, which costs wall-clock time ($K$ blocks each)---\textbf{time-as-cost replaces provenance-as-cost}" (§Import) — satisfiable and dimensionally coherent. The circulation-only decay rule "sitting idle never normalizes imported wealth" (§Import) is consistent with the value-weighted tag-blending stated in the same subsection. |
| 6 | Structure & navigation | 4 | 4 | NO CAP (step-4c clean — all three exhibits render, see below). Prose wayfinding is audit-grade: numbered subsections, a labelled status register with stable IDs, resolving "see \S" cross-refs ("It builds on the transaction model (\S\ref{sec:transactions})" — §opening), and the three now-rendered figures with accurate captions ("Federation custody and attestation topology. Each SCP validator operates an Ed25519 node key ... and a secp256k1 signer" — Fig~\ref{fig:bridge:custody}) placed at the flows they illustrate. +2 from v1's capped 2 — recovery is the missing-figure cap lifting to full prose merit. |
| 7 | Cross-reference & versioning discipline | 4 | 4 | NO CAP (step-4c clean). All three prior dim-7 defects are fixed: the register now carries stable IDs ("Rows carry stable IDs (\textbf{IMP-1}--\textbf{IMP-6}) so that figure captions and cross-references resolve to an exact row" — §Implementation Status) and the fig2 caption resolves to the correct one ("the current implementation releases a factor-1 output (register row IMP-2)" — Fig~\ref{fig:bridge:unwrap}); the `\addlinespace` macro (undefined in the whitepaper preamble, which has no booktabs) is fully removed (0 occurrences, tables use only `\toprule`/`\midrule`/`\bottomrule`, verified drop-in-`\input`-able); versioning is carried by the sibling `changelog.md`. +2 from v1's capped 2. |
| 8 | Prose clarity | 4 | 4 | Implementer-grade sentences; a normative requirement is never ambiguous on first read. "The reserve holds only zero-demurrage coins, so \eqref{eq:bridge:peg} holds over time by construction" (§Peg) states rule-and-rationale in one clause. No re-reading needed to parse what is required. |
| 9 | Rhetorical economy | 3 | 3 | Exhaustive without padding; each subsection earns its length. "time-as-cost replaces provenance-as-cost" (§Import) compresses the whole Sybil-resistance argument into a single load-bearing clause. The +209 words added in v2 are all normative payload (RFC-2119 clause + base-layer CT rewording), no digression. |
| | **Total** | **44** | **44** | At/above the ≥39 audit-grade threshold — advance (pending clean audit sibling). |
