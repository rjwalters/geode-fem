# Scoring — botho-from-the-basics.3 (anvil-primer-v1, /44)

Scored on v3's own merits. The v2→v3 diff is exactly seven hunks (five figure paragraphs + two acronym glosses); for unchanged material this review leans on the prior sibling `botho-from-the-basics.2.review/` (cited per dimension as "v2 review") rather than re-deriving, per the operator's scope note. All seven insertions, and all five exhibit PNGs, were reviewed fresh.

| # | Dimension | Weight | Score | Justification |
|---|---|---|---|---|
| 1 | Pedagogical scaffolding / learnability | 7 | 7 | The unchanged prose spine carries the v2 review's clean top-to-bottom newcomer walk (7/7, byte-identical here), and all five figures land only after their concepts are taught — G4's caption can say "The cluster factor — computed from coin lineage, not holder identity — prices the intake" (— §9.5 Figure 4 caption) precisely because §§9.2–9.5 have already built every element it names. The one preview moment, Figure 2 naming the lattice schemes a subsection early ("Secrets that must last forever — recipient identity (ML-KEM-768 handshake) and minting attribution (ML-DSA-65 signatures)" — §6 Figure 2 caption), glosses each name with its already-taught role and is unpacked by the two subsections immediately below — the signposted, glossed-at-point-of-use pattern the v2 review called exemplary, not a block. |
| 2 | Intuition before formalism | 6 | 6 | Unchanged prose per the v2 review (6/6); the captions extend the pattern rather than diluting it — each is a plain-language re-teach of the mechanism's why, e.g. "Hashpower buys zero consensus votes — a candidate block crosses the wall, voting power never does." (— §8 Figure 3 caption), which restates the decoupling argument with zero notation. No instance of notation dumped without a prior intuition found among the insertions. |
| 3 | Worked-example / walkthrough concreteness | 5 | 5 | The v2 review's cured traces (demurrage anchor, 611M bridge, complete fee instance — "a small multi-output penalty (4, for the two outputs) = 20,000 pico-BTH" — §10 step 2) are byte-identical, and the figures add visual traces on top: the capstone now opens with a phase map ("a miner proposes and SCP finalizes (steps 8–9)" — §10 Figure 5 caption) that makes the eleven-step walkthrough navigable at a glance, and Figure 4 renders the entire §9 money flow as one traceable circuit. |
| 4 | Technical accuracy | 5 | 5 | Reviewer judgment side: the unchanged prose carries the v2 review's number-by-number re-verification (5/5); every caption claim was checked fresh against diagram, prose, and spec — "20% is burned and 80% joins a scheduled slice of the block reward in the lottery pool" (— §9.5 Figure 4 caption) matches 07-monetary.tex:270–276 and the ramp-to-50% at :502; the 1,088-byte ciphertext and ~3.3 KB minting signature in Figure 2's diagram match 04-cryptography.tex:91 and 05-transactions.tex:98 (3,309 bytes). Figure 1's caption compression ("derives a fresh one-time output key from the recipient's two published keys via a one-way handshake" — §3 Figure 1 caption) is lossy-but-true — the diagram itself shows the precise view-key/spend-key split (comment 3). |
| 5 | Spec cross-reference discipline | 5 | 5 | The duplication sweep on the only new material is clean: all five captions restate audited body prose with no formula, no derivation, no normative table — the model teach-then-point moves survive untouched ("The exact curve is a log-domain sigmoid — see the whitepaper" — §9.3), and captions cite primer-internal sections ("classical Diffie–Hellman in shape; ML-KEM in Botho, Section 6" — §3 Figure 1 caption), never spec sections, so they cannot duplicate or mis-point. Unchanged body's clean sweep and verified blockquote targets carry over from the v2 review (5/5). |
| 6 | Audience calibration | 4 | 4 | Both v2 residuals are cured verbatim at point of use: §8 now gives ASIC the full BFT/MEV treatment ("chips custom-built for a single algorithm" — §8) and the §11 map row is glossed with the acronym retained for spec findability ("trusted-execution-environment (TEE, secure-enclave) approaches" — §11). No new unglossed jargon rides in with the captions — every acronym they use (ML-KEM, ML-DSA, CLSAG, SCP, UTXO) is either taught before the figure or role-glossed inline in the caption. |
| 7 | Structure & navigation | 4 | 4 | The v2 wayfinding apparatus is byte-identical ("each later section will point back at them" — §1), and the figures strengthen it: Figure 5 is an explicit map-before-the-walk ("The whole payment at a glance" — §10 Figure 5 caption) doubling as the recap, Figure 3 marks the §§7–8 conceptual boundary, and Figure 4 gives §9's five subsections a single synthesis image right before §9.6 argues why the shape is necessary. |
| 8 | Prose clarity | 4 | 4 | Unchanged prose per the v2 review (4/4); the captions read as single teaching sentences that land on first pass, e.g. "everyone else sees only an unlinkable random-looking key" (— §3 Figure 1 caption, closing the caption exactly where the prose paragraph above it closes). Figure 4's caption is the densest sentence the revision adds, but every clause restates material taught in the pages immediately above it and the diagram beside it disambiguates (comment 4); no instance of a sentence requiring re-reading to parse found among the insertions. |
| 9 | Rhetorical economy | 4 | 4 | +278 words over v2, all load-bearing: five one-sentence caption re-teaches ("RandomX proof-of-work meters out new coins and the right to propose blocks" — §8 Figure 3 caption) and two one-clause glosses — exactly the two fixes the v2 review priced at one point, nothing more. No instance of padding or non-load-bearing digression found among the insertions; the unchanged body's economy carries over from the v2 review (4/4). |
| | **Total** | **44** | **44** | Advance threshold ≥35. |
