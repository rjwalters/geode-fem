# Line-level audit comments — botho-from-the-basics.3

Keyed to `botho-from-the-basics.3/botho-from-the-basics.md`. All comments are notes; none blocks.

1. **Line 210 (fig1 paragraph, §3)** — Caption restates the immediately preceding "How the classical construction works" paragraph with zero drift; the "(classical Diffie–Hellman in shape; ML-KEM in Botho, Section 6)" parenthetical exactly mirrors the body's forward-pointer two lines above. Placement satisfies the dependency-order discipline (every depicted concept already taught). No action.
2. **Line 387 (fig2 paragraph, §6)** — Caption's three-way assignment (ML-KEM-768 recipient identity / ML-DSA-65 minting attribution / CLSAG sender anonymity) is the spec's hybrid rationale table verbatim (`04-cryptography.tex:326–343`). Note for future passes: the diagram's "only once per block" for ML-DSA-65 follows the spec oracle; if the operator ever reconciles the WP↔code divergence (carried O1) in code's favor, this diagram box and the §6 body both need revisiting together. No action now.
3. **Line 607 (fig3 paragraph, §8)** — "Halt, don't fork" and "hashpower buys zero consensus votes" restate the wall subsection just above. The diagram's "any two quorums share an honest node" states the safety condition as a property (the spec's own summary style, `06-consensus.tex:296`); the body's fuller treatment at lines 503–509 carries the conditional framing, so a reader of the primer gets the assumption structure. No action.
4. **Lines 611–612 (ASIC gloss, §8)** — Reviewer's parenthetical adopted verbatim; accurate, placed at first §8 use. Note: "ASIC-resistance" appears earlier (§2 roadmap bullet) — that earlier use was already reviewed as acceptable in v2 (roadmap-level mention); the gloss lands at the point of substantive use. No action.
5. **Line 834 (fig4 paragraph, §9.5)** — Caption's single sentence packs five audit-verified numbers (factor tilt, 20/80, ramp-to-50%, 4 UTXOs, inverse-factor odds); each traces to a v2-verified claim (D1/D3/S5–S8/S13). "Computed from coin lineage, not holder identity" is the §9 refrain, correctly repeated. No action.
6. **Line 882 (fig5 paragraph, §10)** — Caption is purely structural (phase map), deliberately number-free; the diagram carries the numbers, all matching body steps 1–11 exactly (30+25 / 40 / ~15 / 20 nano / rings of 20 / ~5 KB / 20-80 / 4 winners). The one compression, "quantum-safe forever" (step-11 box), is scoped to recipient linkage by both diagram context and body step 11 — lossy-but-true (findings F5j). No action.
7. **Line 980 (TEE gloss, §11 map)** — "trusted-execution-environment (TEE, secure-enclave)" matches WP §2's own phrasing (`02-related-work.tex:159`); acronym retained for findability as the changelog intends. No action.
