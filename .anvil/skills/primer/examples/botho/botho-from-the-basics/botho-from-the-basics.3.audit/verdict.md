# Audit verdict — botho-from-the-basics.3

**Audit: CLEAN.** Zero unresolved audit critical flags.

- **Critical flags: none.**
  - **"Contradicts cited spec"** (flag 2, ACTIVE — `spec_ref: ../sections/*.tex` resolved to all 18 whitepaper LaTeX sections under `whitepaper/sections/`): **0 contradictions found.** The v3 delta is exactly seven hunks (five figure paragraphs + two acronym glosses); every claim in the five captions AND in the five rendered diagrams (each PNG inspected, each `.mmd` source read) was cross-checked against the resolved spec. None disagrees.
  - **"Subtly-wrong intuition"** (flag 3, always eligible): **0 false simplifications found.** Every diagram-box compression examined resolves to lossy-but-true — the reader's carried-away belief is correct in each case (per-claim table in `findings.md`).

`audit_clean: true`

## Scope and diff verification

`diff` of `botho-from-the-basics.2/botho-from-the-basics.md` against `botho-from-the-basics.3/botho-from-the-basics.md` was re-run by this auditor and confirms the changelog's assertion: **exactly 7 hunks** — five pandoc implicit-figure paragraphs (fig1 after §3's classical-construction paragraph; fig2 after §6's design-rule paragraph; fig3 at the §8 mining-weight/consensus-weight boundary; fig4 at the end of §9.5; fig5 at the head of §10) and two one-clause glosses (§8 ASIC expansion; §11 map-table TEE gloss). All other prose is byte-identical to the v2 body, which carries a clean audit (`botho-from-the-basics.2.audit/`: 18/18 delta claims + 15/15 carried-sample claims verified, 0 flags). **The v2 clean verdict therefore carries for the unchanged prose; this audit's new material is the figure/caption claim surface and the two glosses.**

The five PNGs render their `.mmd` sources faithfully (verified visually); the audit below treats caption text and diagram text as one claim surface per figure.

## The v3 claim surface — verified

1. **Figure 1 (stealth-address flow, §3)** — VERIFIED. Caption and diagram restate the taught flow exactly: address = two published keys (view for recognizing, spend for spending); one-way handshake against the view key (classical DH shape; ML-KEM-768 in Botho, forward-pointed to primer §6); shared secret + spend key → fresh one-time output key; on-chain output = one-time key + amount commitment + KEM ciphertext (matches WP §5's byte table row "Output × 2 (commitment, key, ciphertext)", `05-transactions.tex:420`); recipient redoes the handshake with the private view key, matches "mine," decrypts the amount, derives the one-time private key; non-matches are unlinkable random-looking keys. All per WP §4 (Key Hierarchy, Post-Quantum Stealth Addresses) and the v2-audited body §3/§10 prose.
2. **Figure 2 (hybrid PQ envelope, §6)** — VERIFIED. The permanent-vs-decaying split is WP §4's own rationale table verbatim (`04-cryptography.tex:326–343`, tab:hybrid: recipient identity / Permanent / ML-KEM-768; sender identity / Ephemeral / CLSAG; minting authority / Permanent / ML-DSA-65). Numbers: ML-KEM-768 ciphertext 1,088 B per output (v2 audit S2: `03-preliminaries.tex:145`, `04-cryptography.tex:91`) and "the biggest line item" holds against §5's byte table; ML-DSA-65 ~3.3 KB (3,309 B, v2 S3) "only once per block" holds — minting-only per `04-cryptography.tex:314–316` with one minting transaction per block (`05-transactions.tex:133`); CLSAG ~700 B/input, PQ rings ~50× (~35 KB), multi-input >100 KB — all in `04-cryptography.tex:345–350` and `05-transactions.tex:430–431` (v2 S1/S4). "Desktop and phone nodes would stop being realistic" extends the spec's "making desktop nodes impractical" (`04-cryptography.tex:350`) with the phone-node clause already present in the v2-audited body (line 439–440) — lossy-but-true. "Decades stale before a quantum computer could recover it" is the spec's own 2025→2045 framing (`04-cryptography.tex:352–357`). "Documented migration path" is documented: `11-implementation.tex:419–421` ("Future protocol upgrade may introduce post-quantum ring signatures once…") and `13-conclusion.tex:72–73`.
3. **Figure 3 (SCP/mining decoupling, §§7–8)** — VERIFIED. "Any two quorums share an honest node" is spec wording verbatim (`06-consensus.tex:296`; the assumption structure — quorum intersection + Byzantine threshold — at `:92–94`; the diagram makes the same compression the spec's own summary and the unchanged body §7 make). "No reorgs of externalized blocks, ever": `06-consensus.tex:357` ("Once a block is externalized at height h, no reorganization can replace…") and `:444`. "FINAL, seconds after proposal": phase-latency table `:397` (Externalize <1 s) + the body's "handful of seconds." "Halt, don't fork (safety over liveness)": `:105–107` ("preserves safety by halting"). "51% of hashrate cannot rewrite history" + "hashpower buys ZERO consensus votes": WP §9 "51% Attack Resistance" (`09-security.tex:409–424` — "Attacker can flood proposals but cannot force finalization") and `:690` ("Even with majority hashpower, attacker cannot force quorum to finalize different block"). "Coin ownership buys zero too (not proof-of-stake)": unchanged body §8 claim (v2-audited) consistent with the quorum-based tolerance row (`06-consensus.tex:467`). "No ASIC edge, linear rewards": v2 D16 (`07-monetary.tex:430, :434`). "Founding a new cluster": `05-transactions.tex:133` ("Each minting transaction creates a new cluster").
4. **Figure 4 (anti-hoarding money flow, §9)** — VERIFIED. Factor ladder 1×/3.5× at ~100k/past 5× toward the 6× ceiling: v2 D11/D12/S13 (`05-transactions.tex:216–237`). Demurrage ~2%/yr-idle at factor 6, factor-1 exempt: v2 D1/D3 (spec + `cluster-tax/src/demurrage.rs`). "Miners never receive fees — no MEV motive": `07-monetary.tex:407` ("minter_income = R(h) (no fees)"), `:304` ("Anti-MEV: No miner incentive to reorder transactions"), `10-economics.tex:27–29, :259` (fee destination: burned, not miners). 20% burn / 80% pool, emission slice ramping to 50%, 4 UTXOs/block, seed = previous finalized block's hash, odds = value × inverse-factor tilt (6× per-BTH at factor 1 vs 6): v2 S5/S6/S7/S8. "Splitting into a million UTXOs changes nothing" restates the body's value-weighting claim (linearity, v2 S8); the body's own parenthetical carries the dust-floor/maturity caveat. "20% burned — a rebate to every holder equally" is the body's §9.5 sentence verbatim (line 793–794, v2-audited); lossy-but-true (pro-rata deflation).
5. **Figure 5 (capstone timeline, §10)** — VERIFIED. Every number matches the v2-audited capstone exactly: 30 + 25 BTH inputs; 40 BTH to Ben; ~15 BTH change (55 − 40 − 2×10⁻⁸ ≈ 15 ✓); factor 1 → 20 nano-BTH fee, no demurrage (v2 D9/D3); two CLSAG rings of 20 with key images (v2 D15/S1); one aggregated Bulletproof (`05-transactions.tex` byte table: "Bulletproof (2 outputs, aggregated)"); value-weighted tag blend (WP §5 tag propagation); Dandelion++ stem→fluff (WP §8); ~5 KB tx (v2 D10, N1 size note carried); ML-DSA-65 minting transaction (WP §4 Minting Signatures); SCP nominate/ballot/externalize, final in seconds; 20/80 split, 4 winners tilted small (v2 S5/S8). "Mine — 40 BTH, quantum-safe forever" compresses the body's step-11 claim (quantum adversary cannot link the output to Ben's address; the handshake was post-quantum) — the claim is scoped to recipient linkage in both diagram context and body, and amounts are information-theoretically hidden (tab:hybrid); lossy-but-true.
6. **Gloss 1 (§8)** — VERIFIED. "ASICs (application-specific integrated circuits — chips custom-built for a single algorithm)" is the correct expansion and an accurate one-clause definition.
7. **Gloss 2 (§11 map table)** — VERIFIED. "Trusted-execution-environment (TEE, secure-enclave) approaches" matches WP §2's own terminology ("privacy through trusted execution environments (TEEs)", `02-related-work.tex:159–162`; SGX secure enclaves `:56–75`).

## Top audit priorities for the reviser / operator

No blocking items. In descending order:

1. **Carried operator items (unchanged, no primer edit required)**: O1 — WP §4 "Minting Signatures" vs live-code divergence on ML-DSA-65's role (fig2 and fig5 follow the declared oracle, as the body does); O2 — WP-internal 5s/3s and CLSAG byte-figure inconsistencies; N1 — the 2-in-2-out size tension behind the "~5 KB" figure fig5 restates.
2. **N2 (minor, optional, carried)**: the capstone's absolute "never how much" hedge opportunity — unchanged in v3 and outside its operator-directed scope; the figures do not repeat the "never how much" phrasing, so no new exposure.
